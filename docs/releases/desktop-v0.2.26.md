# 寻鹿桌面端 0.2.26

修复通知库与院校库不能加载的问题，兼容网站当前的分页和分片筛选数据接口。

首次连接失败后，点击重试同步会重新核验账号并恢复申请关联。离线时保留申请、备注和本机草稿，并区分待同步与通知已撤回；经过核验的标题仅供当前账号在本机识别申请。

原生网络连接复用，减少重复连接开销。安装前请保存编辑并退出寻鹿；覆盖安装保留原本机资料。

本包为手动下载更新，沿用原 Minisign 签名，Windows Authenticode 未签名，不通过 Stable 自动更新推送。
