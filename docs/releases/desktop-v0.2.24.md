# 寻鹿 SeekOffer 0.2.24 迁移测试版

修复网站与桌面之间的日程和导师联系人同步：保留日程分类、优先级、长备注、删除标记和导师跟进字段，统一按同步基线合并，并在并发冲突时保留本机修改。删除事项不再作为普通记录重新显示；云端取消完成状态可正确同步。

保留原有用户编号、Clerk系统浏览器登录、申请备注双向同步、本机档案和加密凭据。网络失败时不会清空本机内容或报告保存成功。此包不迁回Supabase，不自动开通购买、支付、退款或发送邮件。

此版本为 internal-test 候选，尚未公开发布或替换本机安装。更新包使用原Minisign密钥签名；Windows Authenticode仍需单独满足现有Stable发布要求。原Stable更新地址没有在本次本地打包中切换。
