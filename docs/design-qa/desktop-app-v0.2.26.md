# 0.2.26 验收

旧 0.2.25 编译包已复现两项故障：首载账号核验失败后重试无法恢复；原生通知列表拒绝当前分片元数据协议。

修复后的编译页面通过账号重新关联、申请与备注保留、通知列表呈现、明确撤回后移除旧标题检查。6 个测试文件、31 项单元与集成测试通过；TypeScript、修改文件 ESLint 通过。

独立原生 WebView2 实测：16 条当前列表 11,854 字节，列表请求 1,998ms；三个元数据分片并行请求 1,454ms。真实通知库页面渲染通过，按 ID 详情一致，无关地址被拒绝。合成账号的日程新建、扩展字段、删除与 402 后草稿续传通过。没有真实业务写入。

这些是当前网络下的单次实测，不是速度保证。验收包使用独立 identifier 和空浏览器档案；安装包沿用 com.seekoffer.desktop，无验收调试配置。

用户已在原安装目录更新后的 0.2.26 确认：申请学校名称和通知列表恢复。未完成 24 小时观察。
