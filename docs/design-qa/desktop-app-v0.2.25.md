# 0.2.25 验收

34项工作台、同步协调和额度测试通过；TypeScript与修改文件ESLint通过。

独立原生WebView2验收通过：Rust公开16条列表、按ID详情和无关地址拒绝；真实编译页面在合成认证IPC/API下新增日程、保留联系扩展字段、删除后重载、402本机草稿保留、可见性切换不重试、一分钟后显式续传。未使用真实账号或生产写入。

未完成：新包安装后真实账号复验、Windows休眠与托盘提醒、跨机器私密资料恢复、24小时观察。

验收程序有独立identifier与仅回环调试配置；发布安装包使用com.seekoffer.desktop且不带该调试配置。不替换用户现有程序或数据。
